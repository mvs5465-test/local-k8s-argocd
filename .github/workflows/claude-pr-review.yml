name: Claude PR Review

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  review:
    uses: mvs5465-test/.github/.github/workflows/claude-pr-review.yml@main
    permissions:
      contents: write
      pull-requests: write
      issues: write
      id-token: write
    secrets: inherit
