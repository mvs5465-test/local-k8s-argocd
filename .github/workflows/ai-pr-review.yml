name: AI PR Review

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  ai_review:
    permissions:
      contents: read
      pull-requests: write
      issues: write
      statuses: write
      id-token: write
    uses: mvs5465-test/.github/.github/workflows/ai-pr-review.yml@main
    with:
      reviewer: codex
    secrets: inherit
